name: Deploy to Production (Manual)

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Ensure platform-specific dependencies
        run: npm install @rollup/rollup-linux-x64-gnu @swc/core-linux-x64-gnu @esbuild/linux-x64

      - name: Build frontend
        run: npm run build

      # 🚀 FRONTEND DEPLOY (SFTP via SSH)
      - name: Deploy Frontend
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 145.79.211.27
          port: 65002
          username: u425470620
          password: ${{ secrets.SSH_PASSWORD }}
          source: "dist/*"
          target: "/home/u425470620/domains/dream60.com/public_html"
          strip_components: 1
